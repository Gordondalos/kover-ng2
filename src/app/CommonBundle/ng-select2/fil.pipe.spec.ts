/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FilPipe } from './fil.pipe';

describe('Pipe: Fil', () => {
  it('create an instance', () => {
    let pipe = new FilPipe();
    expect(pipe).toBeTruthy();
  });
});
